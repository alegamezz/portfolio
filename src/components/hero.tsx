import React from "react";

// Hero Section
const Hero: React.FC = () => {
  return (
    <section className="flex flex-col justify-center items-center text-center">
      
    </section>
  );
};

export default Hero;